<!-- AI_GENERATE_START -->
# 文件变更与交付规则

## AI 注解

修改前列出实际文件。普通文本使用语法安全、纯 AI_GENERATE_START 与 AI_GENERATE_END 两端标记，每种恰好一次，不追加日期、编号或后缀；已有合法标记保持原样。

Skill 的 YAML frontmatter、脚本 shebang 等语法必须在标记前。严格 JSON、锁文件、source map、压缩文件、校验签名和二进制禁止插注释；在同次变更的根 ai-changes-audit.md 按精确仓库相对路径逐项说明原因，审计文件本身也要标记。

交付、commit、push 或 MR 前，对实际触碰的现存文件清单运行：
`C:\Users\zxxy\.codex\skills\auto-generate-tag\scripts\validate-ai-tags.ps1`。
删除文件单列在变更清单；移动文件验证目标路径。校验不通过先修复。只有用户明确免除本次注解时才能跳过。

工具存在于当前 Windows 环境。其他开发环境须先提供等效且已验证的注解 Gate，不能假称运行了不存在的脚本；[项目检查](../../../scripts/README.md) 提供可移植到安装 PowerShell 的环境的结构与基础标记检查，不取消当前用户指定的全局 Gate。

## 验证与交付

- 运行 [项目检查脚本](../../../scripts/check-project.ps1)，核对目录、引用、需求版本配对与标记。
- 开发中运行定向测试；适用的 typecheck、完整回归及生产 build 在提交前统一执行一次。源码变化后按影响补验。
- 骨架、文档不运行虚构应用构建；模板中的验收项不是已经通过的测试。
- 一次性测试、探测与完整日志放在 D:\test\pstock\任务；正式维护的测试进入对应工程测试目录。
- 检查通过后审查实际差异和范围；只暂存本次文件，不丢弃他人改动。
- commit、push、MR、合并、部署按当前明确授权执行，授权范围内不重复确认；不把某一步成功等同后续成功。
- 交付记录文件、检查命令/退出码、数量、失败摘要、证据路径与未执行项。禁止伪造测试、截图、提交或线上通过状态。
<!-- AI_GENERATE_END -->
