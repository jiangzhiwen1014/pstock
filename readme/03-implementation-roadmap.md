<!-- AI_GENERATE_START -->
# PStock 建设顺序

## 1. 本次交付与后续任务

本次只完成 `LAYOUT-001`。其余任务是后续开发的建议顺序，不代表已经授权实施或已经通过验证。

| 任务 ID | 状态 | 工作范围 | 依赖 | 完成证据 |
|---|---|---|---|---|
| LAYOUT-001 | 已形成文档 | 根 README、readme 导航、目录结构、模块边界、建设顺序 | 已读取原规划任务及空仓库 | 文档链接与注解校验；提交和推送结果以 Git 实际记录为准 |
| BUILD-001 | 待确认 | 明确交易模式与运行约束；验证自建/底座路线、技术栈和数据来源 | LAYOUT-001 | 真实交易案例形成验收样本；选型决策记录；固定候选版本与探测结果 |
| BUILD-002 | 待开发 | 创建前端、API、worker 最小入口及契约、配置、正式测试基础 | BUILD-001 | 两类后端入口调用同一用例；无真实凭据入库；可启动及停止的证据 |
| BUILD-003 | 待开发 | 证券身份、权限、数据质量、个人档案、账户与现金持仓导入 | BUILD-002 | 样本账目可核对；重复导入不重复入账；错误来源或过期数据可见 |
| BUILD-004 | 待开发 | 公司研究、外围影响、决策记录及组合风险 | BUILD-003 | 一条信息能关联公司、持仓与原有判断；证据、反证与历史版本可查看 |
| BUILD-005 | 待开发 | 策略版本、历史回测、独立持续模拟账户 | BUILD-003，策略与交易规则确认 | 固定样本可复现；无未来信息；不可成交有原因；重启不重复成交 |
| BUILD-006 | 待开发 | 持续更新、报告、提醒与长期复盘 | BUILD-004；涉及模拟的报告另依赖 BUILD-005 | 失败后补齐；重复事件可合并；AI 不可用仍有事实；前后判断可比较 |
| BUILD-007 | 待评估 | 深化商品联动、分红税后情景、高级因子等专题 | 相应数据与规则验证；BUILD-005/006 | 每个专题有独立需求、来源、验证样本和失效边界 |

任务按依赖执行，存在可拆分项也不默认启用多 Agent。代码目录在对应任务开始时创建；文档目录可先建立该需求的设计和验收材料。

## 2. 保留前次需求编号

下表是原规划草案到目标目录的追踪，范围、参数和验收细节仍需在正式开发前确认。新增核心领域暂用目录名识别，后续正式立项时分配新需求号，不挪用旧编号。

| 原需求 ID | 原主题 | 主要落点（领域目录简称） | 建设任务 |
|---|---|---|---|
| PS-001 | 个人账户与持仓 | `profile/`、`portfolio/` | BUILD-003 |
| PS-002 | 数据中心 | `market_data/`、数据适配器 | BUILD-003 |
| PS-003 | 策略与评分 | `strategy/` | BUILD-005 |
| PS-004 | 历史验证 | `simulation/`、回测适配器 | BUILD-005 |
| PS-005 | 模拟账户 | `simulation/`、`portfolio/` | BUILD-005 |
| PS-006 | 每日报告 | 应用层聚合、`jobs/`、前端 | BUILD-006 |
| PS-007 | 信息哨兵 | `monitoring/`、通知适配器 | BUILD-006 |
| PS-008 | 期货联动 | `influence/`、`market_data/` | BUILD-004 基础关系，BUILD-007 专题深化 |
| PS-009 | 分红税后分析 | `portfolio/`、`market_data/` 的规则与数据，应用层情景编排 | BUILD-007 |

## 3. 开发前待确认项

| 事项 | 对结构或行为的影响 | 未确认时的处理 |
|---|---|---|
| 最近一笔实际交易的完整过程 | 决定决策卡、策略和观察周期 | 保留通用证据/条件模型，不预设趋势、价值或事件策略 |
| 资金、周期、最大回撤、看盘时间 | 决定账户约束、报告节奏、提醒方式 | 保留未设置，不写入附件示例值 |
| 本机使用还是长期运行服务器 | 决定启动方式、认证、任务恢复和部署配置 | 只规划 API/worker 两类入口 |
| 数据预算及实际覆盖 | 决定历史验证范围、缓存和备用源 | 以可验证数据为边界，不承诺完整历史区间 |
| 自建主应用还是改造 TSP | 决定初始包结构、迁移成本及许可核对范围 | 文档保留逻辑边界，不提前复制源码 |
| 正式回测引擎 | 决定回测适配层实现 | 相同样本评估后选一个；持续模拟和回测沿用统一规则 |

## 4. 验收与失败处理

目录落地时检查：文档链接可访问、规划与实有文件区分明确、同一业务没有多个权威归属、所有 AI 文件通过标记校验、提交仅包含本任务文件。

后续开发分别验证：数据和规则可追溯、账户隔离、计算复现、幂等恢复、缺失降级及真实页面闭环。具体测试跟随功能的验收样本设计，不为本次纯文档变更运行空构建或虚构功能测试。

若某候选组件接口、许可或样本结果不满足条件，停留在选型任务，替换适配实现或调整路线，不通过改动持仓、数据或收益口径来迁就引擎结果。

## 5. 迁移与回退

当前没有已有代码和数据需要搬迁，本次提交只增加文档；回退可通过新的反向提交撤销相应文档，不使用强制推送改写历史。

后续接入底座先保存可运行基线，再按模块小批量迁移；每批保留旧入口到新入口的对应表和验收结果。切换失败时回到前一可运行版本。

数据库迁移必须单独设计兼容窗口、备份和恢复验证；文件移动不能替代数据回退。第三方替换时保持端口、证券标识和账本规则稳定，并用固定样本对账。

[返回文档导航](README.md)
<!-- AI_GENERATE_END -->
