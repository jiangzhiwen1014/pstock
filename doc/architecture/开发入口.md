<!-- AI_GENERATE_START -->
# Claude Code 与模型开发入口

项目开发工具为 Claude Code，模型可以使用 Claude 或用户配置的 DeepSeek。本仓库只管理项目级规则，不设置提供商、凭据、请求地址或全局插件。

## 加载方式

根 [CLAUDE.md](../../CLAUDE.md) 使用文件导入语法加载 [AGENTS.md](../../AGENTS.md) 和 [项目 Skill](../../skills/pstock-development/SKILL.md)。细则由 Skill 按任务指引读取。

Claude Code 支持在 CLAUDE.md 中通过 @ 路径导入文件；官方说明 AGENTS.md 需要由 CLAUDE.md 显式导入。[官方加载说明](https://code.claude.com/docs/en/memory)

本项目的 `skills/` 是统一规则源，当前通过入口文件导入，不宣称它已注册为斜杠命令。需要原生命令发现时再配置薄入口，避免复制规则正文。[官方 Skill 说明](https://code.claude.com/docs/en/skills)

## 实际接入核验

1. 从项目根目录启动 Claude Code，查看 /context 中的 Memory files，确认 CLAUDE.md 及导入规则。
2. 开工前让当前模型复述本任务的需求 ID、独立版本、状态、写入范围以及需要读取的细则。
3. 切换 DeepSeek 后重查入口是否加载，读取同一任务文件的进度和验证记录；不能只依赖前一模型的口头总结。
4. 导入缺失时手动读取明确的项目文件并修复工具配置，不把规则目录存在当作接入验收通过。

当前完成的是入口文件与静态引用检查，未启动 Claude Code 或 DeepSeek 会话做真实加载验收。Markdown 指令是模型上下文，不是进程级强制拦截；自动检查只能覆盖结构、引用、版本配对与注解，不证明业务正确性或模型一定遵循。
<!-- AI_GENERATE_END -->
